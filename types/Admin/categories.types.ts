export type CategoryTypes = {
  id: string;
  title: string;
  description: string | null;
  image_url: string;
  priority: number;
  is_active: boolean;
  created_at: string;
  updated_at: string;
};